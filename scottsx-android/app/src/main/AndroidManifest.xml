<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Android 13+: without this every push notification is silently
         dropped — including message alerts and the welcome ping. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ScottsTechXApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.ScottsTechX"
        android:largeHeap="true">

        <!-- Firebase Cloud Messaging receiver. Without this entry the
             service class exists but the system NEVER delivers pushes
             to it, so "message arrived" notifications did not work. -->
        <service
            android:name=".data.push.ScottsMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ScottsTechX"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Firebase email verification deep link.
                 When the user taps the verification link in their email,
                 Android opens the app at MainActivity. MainActivity reads
                 the incoming Intent and we trigger FirebaseAuth.getInstance()
                 .currentUser?.reload() to pick up the new isEmailVerified flag. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <!-- Firebase default Dynamic Links host (works out of the box
                     on any Firebase project, no DNS setup required). -->
                <data
                    android:scheme="https"
                    android:host="scottstechx-52bab.firebaseapp.com" />

                <!-- Custom scheme for direct app-only links from the
                     verification email templates if/when configured. -->
                <data android:scheme="scottsx" />
            </intent-filter>
        </activity>
    </application>
</manifest>